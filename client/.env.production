# Production build config (used automatically by `vite build`).
# This Render service serves the React app AND the API from the same origin,
# so the client must call the API with a same-origin relative path.
# Do NOT put localhost here. Overrides the dev value in client/.env.
VITE_API_URL=/api
